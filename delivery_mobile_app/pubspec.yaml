name: delivery_mobile_app
description: Mobile App for Delivery Service

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http:
  intl:
  url_launcher: ^6.1.7
  cupertino_icons: ^1.0.2
  json_annotation: ^4.7.0
  mvc_pattern: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.0.0
  json_serializable: ^6.0.0

flutter:
  uses-material-design: true
